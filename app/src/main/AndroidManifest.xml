<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.itsability"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!--
    https://stackoverflow.com/questions/8077728/how-to-prevent-the-activity-from-loading-twice-on-pressing-the-button
    Activity가 두개 생기는 현상은 위의 링크를 참고하였습니다.
    Activity에 android:launchMode="singleTask"를 추가합니다.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMapActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".MainArActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".MainSearchActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".PlaceDescriptionActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".PlaceSearchActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".PhotoDescriptionActivity" android:launchMode="singleTask"></activity>
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:launchMode="singleTask"></activity>
    </application>

</manifest>